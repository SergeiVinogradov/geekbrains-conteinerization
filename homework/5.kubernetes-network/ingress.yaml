---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: my-redmine.local
    http:
      paths:
      - backend:
          serviceName: redmine-app
          servicePort: 3000
